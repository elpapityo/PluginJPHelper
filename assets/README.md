# Plugin JP Helper assets

`PluginJPHelper.png` is the square installer icon referenced by both `pluginmaster.json` and `PluginJPHelper/PluginJPHelper.json`.

Dalamud installer icons should be square PNG files. Keep this asset at 512x512 or smaller.
